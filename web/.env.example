# Backend API URL (for Vite dev server proxy and production server-side proxy)
# In Docker, this is set automatically to http://server:3000
# For local development without Docker:
VITE_API_URL=http://localhost:3000

# Public URL of the web app (for SvelteKit ORIGIN)
# VITE_API_URL=http://localhost:3000