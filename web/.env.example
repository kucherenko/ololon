# API URL for the Rust backend server
# Default: http://localhost:3000
VITE_API_URL=http://localhost:3000

# Note: API key is now managed via the login form and stored in localStorage
# Set OLOLON_AUTH_TOKEN when starting the backend server